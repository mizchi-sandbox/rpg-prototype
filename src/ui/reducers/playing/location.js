/* @flow */
export type Action = void
export type State = {}
const initialState: State = {}

export default (
  state: State = initialState,
  action: Action
) => {
  return {}
  // switch(action.type) {
  //   default:
  //     return state
  // }
}
